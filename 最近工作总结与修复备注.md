# 最近工作总结与重点修复备注

本文件是近期 `0816Test02` 工作的总结和后续维护重点，当前推送分支为 `FerroKHMag3D`。

---

## 1. 工作范围

近期主要围绕铁磁流体界面不稳定性的 3D 数值案例展开：

- `examples/kh2dStd`：2D Kelvin-Helmholtz 标定；
- `examples/khMag3d`：3D KH 磁流体案例；
- `examples/khMag3dMaxwell`：3D KH + 完整 Maxwell 磁应力算子的主案例；
- `examples/khMag3dMaxwellFix`：边界修复过程中的调试/参考副本；
- `examples/rt2dStd`、`RTDeepSeek2D/3D`、`khMagDeepSeek3D`：相关标定与对照案例。

核心代码变更集中在：

- `examples/khMag3dMaxwell/khMag3dMaxwell.cpp`
- `src/mfield/mfield3d.h`、`src/mfield/mfield3d.hh`
- `src/mfield/mfield2d.hh`
- 若干案例的 ini 与诊断脚本。

---

## 2. 物理与数值方案要点

- D3Q19 求解 NS + 相场；D3Q7 用于磁场。
- 相场采用守恒 Allen-Cahn 形式。
- 磁力采用完整 Maxwell 应力算子：

```text
F_m = scale * ( χ |H| ∇|H| − 0.5 |H|^2 ∇χ )
```

- KH 磁场为水平场，`psi = -H0*x` 在 x 周期缝上是斜坡，需要用“斜坡平移周期边界”，不能直接普通周期卷绕。
- 磁势使用解析扰动势作为初值，再交给有限差分 Gauss-Seidel 求解器，避免 D3Q7 伪时间迭代在水平扭曲周期缝上积累反号分支。

---

## 3. 最重要的边界修复

### 3.1 现象

正式网格在 `x=0` 和 `x=Ni-1` 附近出现左右边界条带/块状伪影；`Bo_m=0` 时同样存在，说明不是磁力项造成，而是 PF/NS 周期边界数据错误。

### 3.2 根因

框架 `FixedPeriodicBoundaryManager::Setup/SetupMPI` 在给周期 ghost 找 source 块时使用：

```cpp
sblock.getSelfBlock().isInside(sourcePos)
```

相邻块共享 ghost 层时，同一个 `sourcePos` 会同时落在两个 selfblock 内，管理器可能先选中相邻块的 ghost 层，把周期 ghost POP 从过期 ghost 复制过来。错误 POP 进入 streaming 后，在左右边界持续放大。

### 3.3 修复

在案例内新增 `SyncPeriodicPops`：

- 按 `GlobalBlockTable` 枚举所有 x/y 周期 ghost；
- 显式把 ghost 坐标 x/y 取模到对侧物理 cell；
- 使用 block 的 base 物理区间反查 source 块；
- 均匀块用 O(1) 查表；
- 发往同一 rank 的 pair 缓冲区合并为单个 MPI 消息，request 数最多为 `nranks-1`；
- 自定义 Waitall 使用显式 `MPI_Status` 数组；
- 不再调用框架的 `NS_Per.Apply()` / `PF_Per.Apply()`，小网格逐位对比结果一致。

小网格验证：`32×8×64`、`np=4`、`t=10`，y 周期缝差从 `~1e-1` 降到 `~1e-16`。

---

## 4. MPI 报错重点注意

### 4.1 报错

```text
Fatal error in PMPI_Waitall: Other MPI error
MPID_nem_lmt_shm_start_send ...
MPIU_SHMW_Seg_detach: unable to remove shared memory - unlink No such file or directory
```

### 4.2 根因

该错误来自 MPICH CH3/nemesis 的共享内存 LMT 传输路径。双向大消息同时发生时，两端可能同时删除同一 shm region，出现 unlink ENOENT。报错中的 `count=176/286` 是框架 `BlockFieldManager` / `BlockLatticeManager` 通信的 request 数，不是案例代码的 Waitall。

### 4.3 当前处理

在 `mpi().init()` 之前设置：

```cpp
setenv("MPIR_CVAR_NOLOCAL", "1", 0);
setenv("MPICH_NOLOCAL", "1", 0);
setenv("MPIR_CVAR_CH3_NOLOCAL", "1", 0);   // MPICH <= 3.2
```

这会关闭 MPICH 的本地共享内存通信路径，从根源避开该竞态。该设置不改变计算结果，只影响传输层。

**注意：**

- 开启 NOLOCAL 后，节点内 MPI 通信可能变慢，本地实测大网格约慢 30%。
- 如果以后集群把 MPICH 升级到 4.x（ch4:ofi），可以删除这三行 `setenv` 再评估性能。

---

## 5. 编译与运行提醒

1. 每次修改源码后必须在集群上重新编译，不要使用旧 `khMag3dMaxwell.exe`：

```bash
cd examples/khMag3dMaxwell
make -j
```

2. 正式运行前确认 ini 中的网格、`Bo_m`、`TotalStep`、`OutputStep` 是目标配置。
3. 大输出目录不进入版本控制，已忽略：

```text
vtkoutput*/
vtidata/
*.vti
*.vtm
*.o
*.exe
*.log
*.pyc
__pycache__/
```

4. 本地保留但未提交的文件：

```text
.merge-work/
khMag_fixes.patch
record.md
```

这些文件已加入 `.git/info/exclude`，不会误提交；确认不需要后可以删除。

---

## 6. 仓库状态与日常操作

当前本地仓库根目录：

```text
/home/dlf/myCode/0816Test02
```

当前分支：

```text
FerroKHMag3D
```

日常提交流程：

```bash
cd /home/dlf/myCode/0816Test02
git status
git add <修改文件>
git commit -m "说明"
git push origin FerroKHMag3D
```

注意：原 `/home/dlf/myCode/.git` 位于只读文件系统，目前只作为历史仓库保留；日常开发和提交请使用 `0816Test02` 这个仓库。
